window.addEventListener('scroll', funtion(){
  let Acerca de = this.document.getElementById('Acerca de')
  let posicionObj1 = Acerca de.getBoundingClientRect().top()
  console.log(posicionObj1)l;
}